# kasir_cepat

Kasir Cepat is easy simple POS system build free for small business, the goals of the app is make a small business can get a POS system for free. the app running on offline (except login). the feature should be simple but important (POS core), no fancy feature so no expenses needed to running the app.


## Getting Started

